#include <iostream>
#include <string>

int main(){
    std::string name = ""; // созлание переменной
    std::cin >> name; // считывание
    std::cout << "Hello world from " << name; // вывод
    return 0;
}
